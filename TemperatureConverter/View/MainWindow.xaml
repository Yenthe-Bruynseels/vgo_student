<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:View"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <LinearGradientBrush x:Key="labelBackgroundBrush" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="cyan" Offset="0"/>
            <GradientStop Color="DarkCyan" Offset="1"/>
        </LinearGradientBrush>
    </Window.Resources>
    <StackPanel>
        <TextBlock Text="Fahrenheit" Background="{StaticResource labelBackgroundBrush}" FontWeight="Bold" Padding="2" FontSize="16">
        </TextBlock>
        <TextBox x:Name="textBoxFahrenheit" FontSize="16" TextAlignment="Center"/>
        <Button Content="Convert to Celsius" Click="ConvertToCelsius"/>



        <TextBlock Text="Celsius" Background="{StaticResource labelBackgroundBrush}" FontWeight="Bold" Padding="2" FontSize="16">
        </TextBlock>
        <TextBox x:Name="textBoxCelsius" FontSize="16" TextAlignment="Center"/>
        <Button Content="Convert to Fahrenheit" Click="ConvertToFahrenheit"/>
    </StackPanel>
</Window>
